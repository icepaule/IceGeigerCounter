# Copy to .env (never commit). Generate secrets locally, e.g. `openssl rand -hex 20` / `openssl rand -base64 32`.
MQTT_BROKER_HOST=host.docker.internal
MQTT_USER=CHANGE_ME
MQTT_PASSWORD=CHANGE_ME
POSTGRES_PASSWORD=CHANGE_ME
API_SECRET=CHANGE_ME
CHIRPSTACK_UI_PORT=8180
