#pragma once
// Copy to secrets.h, fill locally, NEVER commit secrets.h.
static const char WIFI_SSID[] = "CHANGE_ME";
static const char WIFI_PASSWORD[] = "CHANGE_ME";
static const char GW_BRIDGE_HOST[] = "CHANGE_ME";   // host running the ChirpStack Gateway Bridge (UDP 1700)
static const uint16_t GW_BRIDGE_PORT = 1700;
static const char NTP_SERVER[] = "pool.ntp.org";
